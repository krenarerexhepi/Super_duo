We needed to use our API_KEY to build the Footbal app so I put that key in the string.xml file.

For security reasons before to zip project I have removed that key. You can put you key in the api_key variable. 